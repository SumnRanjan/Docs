<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notes</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="main w-full min-h-screen bg-slate-900 flex justify-center items-center flex-col"
    >
      <div class="form container px-40 py-10 text-white">
        <h1 class="text-4xl font-bold text-center m-8">Notes App</h1>
        <form action="/create" method="post">
          <input
            class="block px-3 py-3 bg-slate-800 w-full rounded-lg outline-none"
            type="text"
            name="title"
            placeholder="Title"
          />
          <textarea
            class="block px-3 py-3 bg-slate-800 w-full rounded-lg outline-none mt-3"
            name="details"
            placeholder="Task Details"
            id=""
          ></textarea>
          <input
            class="px-5 py-3 bg-blue-600 rounded-lg mt-3 hover:bg-blue-700 cursor-pointer"
            type="submit"
            name=""
            value="Create Task"
            id=""
          />
        </form>
      </div>
      <div class="px-40 flex flex-wrap gap-6">
        <% if(files.length > 0) { %>
             <% files.forEach((val)=>{ %>
           <div class="w-72 px-5  bg-slate-800 rounded-lg ">
            <h1 class="text-white text-2xl"><%= val  %></h1>
            <div class="flex justify-between  w-full mt-3">
                <a class="text-blue-500 mt-3" href="/files/<%=val  %>">Read More</a>
                <a class="text-blue-500 mt-3" href="/edittitle/<%= val %>">Edit</a>
                <a class="text-red-500 mt-3" href="/delete/<%= val %>">Delete</a>
            </div>
        </div>
        <% }) %>
         <% } else { %>
        <h2 class="relative right-[20vw] text-white font-semibold ">No Task</h2>
        <% } %>
      </div>
    </div>
  </body>
</html>
